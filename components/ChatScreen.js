import styled from "styled-components"

export const ChatScreen = ({ chat, messages }) => {
    return (
        <Container>
            
        </Container>
    )
}

const Container = styled.div `
`